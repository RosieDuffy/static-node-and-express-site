extends layout.pug

block content
    //- Displays 404 Error Message
    .errorText 
        h1=`${err.message} - Status: ${err.status}`
        h2= `:(`
                     
    nav.nav
      a(href='/')
        span &larr;
        |  Back